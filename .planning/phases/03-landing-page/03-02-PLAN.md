---
phase: 03-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/index.astro
  - src/content/reports/2026-vietnam-macro.mdx
  - src/content/reports/2026-fpt-equity.mdx
autonomous: true
requirements: [LND-03, LND-04]
must_haves:
  truths:
    - "Directly below the hero, a WSO partnership band shows the real wso-horizontal.png badge, the OUR PARTNER eyebrow, and the LOCKED context line stating only the two true perks"
    - "A value-proposition / prestige strip (editorial 2-up, no eyebrow, no card-cliché) communicates prestige & credibility in prose"
    - "A featured-reports section renders exactly 3 report rows in the Phase-2 .pub-row visual language, each linking to /publications/[id], read through the getReports() adapter (never astro:content directly)"
    - "Three reports are flagged featured: true (2026-vietnam-macro, 2026-gold-report, 2026-fpt-equity) so the trio is intentional, with newest-first top-up fallback to guarantee 3"
    - "A quiet View all publications → text link routes to /publications"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "WSO band + value-prop strip + featured-reports section appended below the hero"
      contains: "getReports"
    - path: "src/content/reports/2026-vietnam-macro.mdx"
      provides: "featured: true flag"
      contains: "featured: true"
    - path: "src/content/reports/2026-fpt-equity.mdx"
      provides: "featured: true flag"
      contains: "featured: true"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/lib/content.ts"
      via: "getReports() adapter (MOD-02 seam)"
      pattern: "import.*getReports.*from.*lib/content"
    - from: "src/pages/index.astro"
      to: "src/lib/site.ts"
      via: "import WSO_BADGE_SRC"
      pattern: "WSO_BADGE_SRC"
    - from: "featured rows"
      to: "/publications/[id]"
      via: "href to report page"
      pattern: "publications/\\$\\{"
---

<objective>
Append the credibility-proof sections below the hero: the Wall Street Oasis partnership band (LND-03), the prestige value-proposition strip (LND-01 support), and the featured flagship reports trio that routes into the live Phase-2 publications library (LND-04). This is the "show, don't tell" payload — prestige is PROVEN by these sections, not asserted in hero prose. After this plan the page proves SEF is institution-grade and `astro build` is green.

Purpose: LND-03 (WSO partnership credibility, first scroll) + LND-04 (featured work linking into the live library) are the proof points that make a recruiter take SEF seriously and a student trust the work.
Output: three new sections inside `src/pages/index.astro` (below the hero from Plan 01); two report MDX frontmatter edits to flag the intended featured trio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-landing-page/03-CONTEXT.md
@.planning/phases/03-landing-page/03-UI-SPEC.md
@.planning/phases/03-landing-page/03-RESEARCH.md
@.planning/phases/03-landing-page/03-PATTERNS.md
@.planning/phases/03-landing-page/03-01-SUMMARY.md
@.agents/skills/design-taste-frontend/SKILL.md
@.agents/skills/high-end-visual-design/SKILL.md
@src/styles/tokens.css

<interfaces>
<!-- Build against these — no scavenger hunt. -->

Adapter (MOD-02 — the ONLY way to read reports; never import astro:content directly):
  import { getReports } from '../lib/content';   // already sorted newest-first
Featured selection (RESEARCH Pitfall 2 / PATTERNS lines 95-100):
  const picked   = all.filter(r => r.data.featured);
  const rest     = all.filter(r => !r.data.featured);
  const featured = [...picked, ...rest].slice(0, 3);   // featured-first, top up to 3 with newest

Asset (delivered on disk, VERIFIED): WSO_BADGE_SRC = '/logos/partners/wso-horizontal.png' (import from '../lib/site').

.pub-row analog (REUSE verbatim — cover-left/content-right, hairline border-top, hover cover scale(1.03)+title→accent):
  markup: src/pages/publications/index.astro lines 86-133 (article.pub-row → .pub-row__image / .pub-row__content / .pub-row__title / .pub-row__cta; href={`/publications/${report.id}`})
  CSS:    src/pages/publications/index.astro lines 379-542 (.pub-row block) — copy into index.astro scoped <style>
Eyebrow + heading analog: .pub-masthead__eyebrow (lines 210-218) + .pub-masthead__headline (lines 220-227).
Gold hairline analog: .pub-masthead__rule (lines 238-244): border-top 1px var(--color-gold), opacity 0.4, width 60px.

Report files on disk (6): 2026-fpt-equity, 2026-gold-report (already featured:true), 2026-masan-equity,
  2026-mbb-note, 2026-sbv-note, 2026-vietnam-macro.

LOCKED copy (UI-SPEC Copywriting Contract):
  WSO eyebrow: "OUR PARTNER"
  WSO context (ONLY these two perks — do NOT add Bloomberg/bootcamps/etc.):
    "Official partner of Wall Street Oasis — exclusive content and courses, plus mentorship through its network."
  Featured eyebrow: "SELECTED RESEARCH"   Featured heading: "The work speaks first."
  Featured text link: "View all publications →"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag the intended featured trio in report frontmatter</name>
  <files>src/content/reports/2026-vietnam-macro.mdx, src/content/reports/2026-fpt-equity.mdx</files>
  <read_first>
    - src/content/reports/2026-gold-report.mdx (the existing featured: true frontmatter shape to mirror)
    - src/content/reports/2026-vietnam-macro.mdx (frontmatter to edit)
    - src/content/reports/2026-fpt-equity.mdx (frontmatter to edit)
    - .planning/phases/03-landing-page/03-CONTEXT.md (locked decision: featured trio = 2026-vietnam-macro, 2026-gold-report, 2026-fpt-equity)
  </read_first>
  <action>
    Set `featured: true` in the YAML frontmatter of 2026-vietnam-macro.mdx and 2026-fpt-equity.mdx (2026-gold-report.mdx is already featured: true). Match the exact key formatting used in 2026-gold-report.mdx (same indentation/quoting). Change ONLY the featured field — do not touch any other frontmatter or body content. This makes the top-3 selection intentional per the owner's locked decision rather than "newest by accident"; the Task-2 fallback still guarantees 3 if any flag is later removed.
  </action>
  <verify>
    <automated>grep -l "featured: true" src/content/reports/*.mdx | grep -c "vietnam-macro\|gold-report\|fpt-equity"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lc "featured: true" src/content/reports/2026-vietnam-macro.mdx` returns 1.
    - `grep -lc "featured: true" src/content/reports/2026-fpt-equity.mdx` returns 1.
    - Exactly 3 report files now contain `featured: true` (vietnam-macro, gold-report, fpt-equity): `grep -l "featured: true" src/content/reports/*.mdx | wc -l` returns 3.
    - `npx astro check` reports 0 errors (frontmatter still schema-valid).
  </acceptance_criteria>
  <done>Three reports carry featured: true (the locked trio); no other frontmatter changed; schema still validates.</done>
</task>

<task type="auto">
  <name>Task 2: Append WSO band, value-prop strip, and featured-reports section to index.astro</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current state after Plan 01 — append sections below the hero, do not disturb the hero or its styles)
    - src/pages/publications/index.astro lines 86-133 (.pub-row markup to reuse) and 379-542 (.pub-row CSS to copy), lines 210-244 (eyebrow / heading / gold-rule CSS)
    - src/lib/content.ts (the getReports() adapter signature + that reports are newest-first)
    - .planning/phases/03-landing-page/03-UI-SPEC.md lines 324-348 (LND-03 WSO band + LND-04 featured contracts) and 399-405 (value-prop strip) and 422-442 (locked copy)
    - .planning/phases/03-landing-page/03-RESEARCH.md lines 336-348 (featured selection + marquee-adjacent pitfalls) — use the EXACT featured selection snippet
    - .agents/skills/high-end-visual-design/SKILL.md (macro-whitespace --space-4xl/5xl rhythm; tinted not pure-black shadows; cards ONLY when elevation = hierarchy → value-prop strip uses negative space + hairline, NOT 3 equal cards)
    - .agents/skills/design-taste-frontend/SKILL.md (eyebrow budget ≤1 per 3 sections → only WSO + featured carry eyebrows; logo-only; no duplicate CTA intent; top-3 highlights not data-dump)
  </read_first>
  <action>
    In index.astro frontmatter, add `import { getReports } from '../lib/content';` and `import { WSO_BADGE_SRC } from '../lib/site';` (MOD-02 — do NOT import astro:content directly). Compute the featured trio with the EXACT snippet: const all = await getReports(); const picked = all.filter(r => r.data.featured); const rest = all.filter(r => !r.data.featured); const featured = [...picked, ...rest].slice(0, 3).
    Append THREE sections after the hero, in this order (each a top-level <section> with a constrained __inner wrapper, mobile-first):

    (A) WSO partnership band (LND-03) — section background var(--color-surface) or a subtle var(--color-surface-2) tint to set it apart. Centered stack: the gold hairline rule (copy .pub-masthead__rule — border-top 1px var(--color-gold), opacity 0.4, ~60px) above the band; eyebrow "OUR PARTNER" (uppercase, --text-label, letter-spacing 0.12em, --color-ink-muted); the badge <img src={WSO_BADGE_SRC} alt="Wall Street Oasis — official partner" loading="lazy"> at max-height ~48-64px width auto (real delivered file — NO placeholder, NO swap-seam TODO; RESEARCH Pitfall 1 supersedes the stale UI-SPEC note); then the ONE LOCKED context line (Kollektif body, --color-ink): "Official partner of Wall Street Oasis — exclusive content and courses, plus mentorship through its network." State ONLY those two perks. padding-block var(--space-4xl) desktop / var(--space-3xl) mobile.

    (B) Value-proposition / prestige strip (LND-01 support) — NO eyebrow (eyebrow budget spent on WSO + featured). Editorial 2-up: a Cinzel heading (--text-heading, weight 400) + 2-3 short prose points (Kollektif body, each ≤25 words, max-width 65ch) leading with the elite signal SHOWN not shouted (analysts at top global universities, the WSO partnership, institution-grade research) — "quietly elite, restraint over bravado". Group with negative space + a single hairline, NOT card containers and NOT the 3-equal-feature-card cliché (high-end + anti-slop rules). Single column stacked on mobile. padding-block var(--space-4xl) / var(--space-3xl) mobile.

    (C) Featured flagship reports (LND-04) — eyebrow "SELECTED RESEARCH" + Cinzel heading "The work speaks first." (locked). Map over `featured` rendering the Phase-2 .pub-row article markup verbatim (cover-left/content-right, hairline border-top, meta row, title, desc, byline) — drop the data-* filter attrs (no filtering here) but keep the structure; each row's primary link and CTA point to href={`/publications/${report.id}`}. Covers render at the existing row aspect ratio with loading="lazy" (below the fold). Copy the .pub-row CSS block (lines 379-542) into the scoped <style> verbatim (reuse the tuned hover scale(1.03) + title→accent + responsive). Below the rows, one quiet text link "View all publications →" (accent, inherited link style) to /publications — a text link, NOT a third button CTA. Empty/edge: if `featured.length === 0`, render nothing for this section (not expected — 6 reports live). Constrain the featured list inner to max-width 1120px (matches Phase-2 .pub-list).

    All three sections: var() tokens only (decorative gradient/shadow rgba derived from brand hex is the sanctioned exception); Cinzel/Kollektif 400/700 only — never 500/600; macro-whitespace section rhythm via --space-4xl/5xl; tinted (never pure-black) shadows. Extend the single bottom @media (max-width: 767px) block from Plan 01 with these sections' mobile rules (stack, reduce padding to --space-3xl) — do not add a second media block.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -8 && grep -rq "forms.gle/wUxoKXsZkkEF1N1x5" dist/ && ! grep -rq "forms.gle/placeholder" dist/ && echo "BUILD + JOIN SMOKE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` succeeds; SSG renders / with no broken internal links (the 3 /publications/[id] hrefs resolve to real pages).
    - `grep -q "getReports" src/pages/index.astro` succeeds AND `grep -c "astro:content" src/pages/index.astro` returns 0 (MOD-02 seam respected).
    - `grep -q "WSO_BADGE_SRC\|wso-horizontal.png" src/pages/index.astro` succeeds (real badge wired, not a placeholder); `grep -ci "TODO.*badge\|placeholder.*badge" src/pages/index.astro` returns 0.
    - `grep -q "Official partner of Wall Street Oasis — exclusive content and courses, plus mentorship through its network" src/pages/index.astro` succeeds (locked WSO line verbatim); `grep -ci "bloomberg\|bootcamp" src/pages/index.astro` returns 0 (no invented perks).
    - `grep -q "The work speaks first" src/pages/index.astro` and `grep -q "SELECTED RESEARCH" src/pages/index.astro` and `grep -q "OUR PARTNER" src/pages/index.astro` and `grep -q "View all publications" src/pages/index.astro` all succeed.
    - `grep -c "class=\"eyebrow\"\|eyebrow" src/pages/index.astro` shows at most 2 eyebrow instances on the page (WSO + featured only).
    - `grep -q "slice(0, 3)\|slice(0,3)" src/pages/index.astro` succeeds (featured capped at 3 with fallback).
    - `grep -Eq "font-weight:\s*(500|600)" src/pages/index.astro` returns nothing.
    - Built page contains exactly 3 featured row links into /publications/ : `grep -ro "publications/[a-z0-9-]*" dist/index.html | sort -u | wc -l` returns >= 3.
  </acceptance_criteria>
  <done>WSO band (real badge + locked line + 2 true perks), prestige strip (no cards), and exactly-3 featured rows via the adapter linking into the live library are appended below the hero; eyebrow budget ≤2; astro build green; Join smoke passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → content collection | getReports() reads MDX at build time (trusted, owner-authored) |
| page → /publications routes | Internal navigation to Phase-2 pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | featured report data rendered on landing | accept | Only public report metadata (title/division/desc/cover) shown; no PII; same fields already public on /publications |
| T-03-04 | Tampering | broken internal links to /publications/[id] | mitigate | astro build fails on broken internal links — build gate catches dead featured-card links |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new deps) |
</threat_model>

<verification>
- `npx astro check` after Task 1 (frontmatter valid).
- `npx astro build` green after Task 2; the 3 featured /publications/[id] links resolve (build catches dead links).
- Join-URL smoke still passes (regression guard): `dist/` has forms.gle/wUxoKXsZkkEF1N1x5, no forms.gle/placeholder.
- Manual (owner iPhone UAT): WSO band reads premium with the real horizontal logo + only the two true perks; the featured trio looks like the live library and each card opens the right report; value-prop strip uses negative space, not cards.
</verification>

<success_criteria>
- LND-03: WSO partnership band with the real wso-horizontal.png badge + OUR PARTNER eyebrow + locked context line (only the two true perks), gold-hairline framed, visible on first scroll.
- LND-04: exactly 3 featured flagship report rows (intentional trio + fallback), Phase-2 .pub-row language, each linking into /publications/[id], read via getReports() — plus a quiet View all publications → link.
- Eyebrow budget ≤2 on the page; no card-cliché; no invented WSO perks; var() tokens only; 400/700 weights only.
</success_criteria>

<output>
Create `.planning/phases/03-landing-page/03-02-SUMMARY.md` when done.
</output>
