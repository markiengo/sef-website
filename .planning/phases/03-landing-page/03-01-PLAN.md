---
phase: 03-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/site.ts
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
autonomous: true
requirements: [LND-01, LND-02]
must_haves:
  truths:
    - "A visitor opening / sees a full-viewport cinematic hero with the locked Cinzel headline and no scroll required (390px + desktop)"
    - "The hero background is a code-driven animated brand-tone gradient/mesh (green→ivory, faint gold) with static grain — no <video>, no external media file"
    - "Under prefers-reduced-motion the gradient drift and any parallax freeze to a static gradient; hero text appears via opacity only"
    - "Every Join SEF CTA on the page AND in the inherited header/mobile nav points at https://forms.gle/wUxoKXsZkkEF1N1x5 in a new tab; no forms.gle/placeholder remains anywhere in the build"
    - "A secondary Explore work → outline CTA routes to /publications; a quiet View the role → text link routes to JD_URL in a new tab"
  artifacts:
    - path: "src/lib/site.ts"
      provides: "Single source of truth for JOIN_FORM_URL, JD_URL, WSO_BADGE_SRC, UNIVERSITIES[]"
      exports: ["JOIN_FORM_URL", "JD_URL", "WSO_BADGE_SRC", "UNIVERSITIES"]
      contains: "forms.gle/wUxoKXsZkkEF1N1x5"
    - path: "src/pages/index.astro"
      provides: "Landing page with hero section + CTA cluster (replaces Phase-1 placeholder)"
      min_lines: 120
      contains: "hero__bg"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Header/mobile Join links refactored onto JOIN_FORM_URL"
      contains: "JOIN_FORM_URL"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/lib/site.ts"
      via: "import JOIN_FORM_URL, JD_URL"
      pattern: "import.*JOIN_FORM_URL.*from.*lib/site"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/lib/site.ts"
      via: "import JOIN_FORM_URL"
      pattern: "import.*JOIN_FORM_URL.*from.*lib/site"
---

## Phase Goal

**As a** prospective high-school or incoming-college student, **I want to** land on the SEF root URL and be hit with a cinematic, quietly-elite hero and an obvious way to apply, **so that** I immediately want to belong to — and apply to — SEF.

<objective>
Ship the conversion core of the landing page: the centralized links module, the BaseLayout Join-link refactor, and the cinematic code-driven hero with its Join/Explore/JD CTA cluster. This is the thinnest end-to-end shippable slice — after this plan the page already converts (a visitor can land, read the value prop, and reach the Google Form), and `astro build` is green.

Purpose: LND-01 (cinematic hero, no scroll) and LND-02 (prominent Join SEF → Google Form) are the #1 recruitment-conversion path — the owner's single most important surface.
Output: `src/lib/site.ts` (new), `src/layouts/BaseLayout.astro` (2-line refactor), `src/pages/index.astro` (placeholder replaced with BaseLayout wrapper + hero section + scoped styles).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-landing-page/03-CONTEXT.md
@.planning/phases/03-landing-page/03-UI-SPEC.md
@.planning/phases/03-landing-page/03-RESEARCH.md
@.planning/phases/03-landing-page/03-PATTERNS.md
@.planning/phases/03-landing-page/03-VALIDATION.md
@.agents/skills/emil-design-eng/SKILL.md
@.agents/skills/design-taste-frontend/SKILL.md
@.agents/skills/high-end-visual-design/SKILL.md
@src/styles/tokens.css
@src/styles/global.css

<interfaces>
<!-- The executor builds against these — no codebase scavenger hunt needed. -->

site.ts must export (exact values, verbatim from RESEARCH §Code Examples / PATTERNS):
  JOIN_FORM_URL = 'https://forms.gle/wUxoKXsZkkEF1N1x5'
  JD_URL        = 'https://shorturl.at/GOnfJ'
  WSO_BADGE_SRC = '/logos/partners/wso-horizontal.png'
  UNIVERSITIES  = readonly array of { name, file } (8 entries — see PATTERNS lines 54-63; consumed by Plan 03, declare it now)
  NO HERO_VIDEO_SRC / HERO_POSTER_SRC (hero is code-driven).

BaseLayout.astro current state (VERIFIED on disk):
  - line 41: desktop nav Join → href="https://forms.gle/placeholder"
  - line 69: mobile nav Join SEF → href="https://forms.gle/placeholder"
  - both already carry target="_blank" rel="noopener noreferrer" — keep them.

Inherited tokens to USE (never hardcode hex): --color-surface (#F4EEE5 ivory), --color-brand (#00845D green),
  --color-accent (#195042 link/fill), --color-gold (#C89D29 decorative), --color-ink (#1D3937), --color-ink-muted (#54675E),
  --font-display (Cinzel), --font-body (Kollektif), --text-display (clamp 48→80px), --text-body (16px), --text-label (13px),
  --space-* (8pt scale), --ease-cinematic, --ease-standard, --duration-fast/standard/slow/cinematic.

Analog (mirror for frontmatter, BaseLayout wrapper, scoped <style>, is:inline island, responsive @media):
  src/pages/publications/index.astro — note index.astro is at depth 1 so imports are '../layouts' / '../lib' (NOT '../../').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/site.ts and refactor BaseLayout Join links</name>
  <files>src/lib/site.ts, src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/lib/content.ts (the sibling src/lib module convention: leading block comment naming the module's single responsibility + swap contract, then export const)
    - .planning/phases/03-landing-page/03-PATTERNS.md lines 33-65 (exact site.ts contents) and 181-195 (BaseLayout edit)
    - src/layouts/BaseLayout.astro (the file being edited — see the two forms.gle/placeholder strings at lines 41 & 69)
  </read_first>
  <action>
    Create src/lib/site.ts as the single source of truth for external links + landing asset/data, mirroring content.ts's module convention (leading block comment naming its responsibility + the "owner swaps in one place" contract, then plain export const). Export exactly: JOIN_FORM_URL = 'https://forms.gle/wUxoKXsZkkEF1N1x5'; JD_URL = 'https://shorturl.at/GOnfJ'; WSO_BADGE_SRC = '/logos/partners/wso-horizontal.png'; and UNIVERSITIES as a `readonly` (`as const`) array of 8 { name, file } objects using the EXACT names and filenames from the CONTEXT asset manifest / PATTERNS lines 54-63 (Berkeley uc-berkeley.png, NYU new-york-university.webp — the only .webp, UNC unc-chapel-hill.png, Melbourne university-of-melbourne.png, Amsterdam university-of-amsterdam.png, Marquette marquette-university.png, Finance & Marketing university-of-finance-marketing.png, VinUniversity vinuniversity.png), ordered as listed. Add the note comment that there is NO HERO_VIDEO_SRC/HERO_POSTER_SRC (hero is code-driven). UNIVERSITIES is declared now even though Plan 03 consumes it (interface-first ordering).
    Then in BaseLayout.astro: add `import { JOIN_FORM_URL } from '../lib/site';` to the frontmatter, and replace BOTH hardcoded href="https://forms.gle/placeholder" strings (lines 41 desktop, 69 mobile) with href={JOIN_FORM_URL}. Keep the existing target="_blank" rel="noopener noreferrer" on both. No other BaseLayout change — no CSS, no markup-structure edits.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const" src/lib/site.ts` returns >= 3 (JOIN_FORM_URL, JD_URL, WSO_BADGE_SRC) plus the UNIVERSITIES export.
    - `grep -q "forms.gle/wUxoKXsZkkEF1N1x5" src/lib/site.ts` succeeds; `grep -q "shorturl.at/GOnfJ" src/lib/site.ts` succeeds.
    - `grep -q "new-york-university.webp" src/lib/site.ts` succeeds (NYU webp present); UNIVERSITIES has exactly 8 entries.
    - `grep -c "HERO_VIDEO_SRC\|HERO_POSTER_SRC" src/lib/site.ts` returns 0 except inside the explanatory NO-... comment (no actual export).
    - `grep -q "JOIN_FORM_URL" src/layouts/BaseLayout.astro` succeeds; `grep -c "forms.gle/placeholder" src/layouts/BaseLayout.astro` returns 0.
    - `npx astro check` reports 0 errors.
  </acceptance_criteria>
  <done>site.ts exists with all 4 exports + correct literal values; BaseLayout's two Join links import JOIN_FORM_URL and no placeholder string remains; astro check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace index.astro placeholder with the cinematic hero + CTA cluster</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (the Phase-1 placeholder being replaced)
    - src/pages/publications/index.astro lines 1-41 (frontmatter + BaseLayout wrapper + scoped-style page pattern) and lines 144-189 (is:inline island form)
    - src/pages/publications/[id].astro lines 72-80 (CTA markup with inner label span + arrow span) and lines 215-252 (.report-pdf-cta CSS to copy for .cta--primary)
    - .planning/phases/03-landing-page/03-RESEARCH.md lines 168-241 (Patterns 1-4: gradient mesh, grain, parallax, text entrance) and lines 294-302 (anti-patterns)
    - .planning/phases/03-landing-page/03-UI-SPEC.md lines 181-273 (full Hero Contract incl. mobile spec + contrast rule) and 422-442 (locked copy)
    - .agents/skills/emil-design-eng/SKILL.md (transform/opacity-only, :active scale(0.98), custom easing, stagger 60ms, reduced-motion = gentler not zero, hover:hover gate)
  </read_first>
  <action>
    Replace the entire placeholder index.astro. Frontmatter: import BaseLayout from '../layouts/BaseLayout.astro' and { JOIN_FORM_URL, JD_URL } from '../lib/site' (depth-1 paths). Wrap in <BaseLayout title="Saigon Equities Forum — Vietnam's markets, read by its sharpest students" description="A student-led forum for serious equity and macro research on Vietnam."> (use the locked hero copy for SEO).
    Build ONLY the hero section this plan (later plans append sections below it). Structure per UI-SPEC Hero Contract: <section class="hero"> containing (1) <div class="hero__bg" aria-hidden="true"> with a nested <div class="hero__grain" aria-hidden="true"></div>, and (2) <div class="hero__content"> with exactly these elements in order — Cinzel headline (LOCKED: "Vietnam's markets, read by its sharpest students.", max 2 lines), Kollektif subhead (LOCKED: "A student-led forum for serious equity and macro research on Vietnam."), and a CTA cluster div containing: primary <a href={JOIN_FORM_URL} target="_blank" rel="noopener noreferrer" class="cta cta--primary">Join SEF</a>, secondary <a href="/publications" class="cta cta--outline">Explore work →</a>, and a quiet tertiary <a href={JD_URL} target="_blank" rel="noopener noreferrer" class="cta-link">View the role →</a>. NO eyebrow in the hero. Max 4 text elements (anti-slop). Arrow glyphs in their own <span aria-hidden="true"> for the translateX hover.
    Scoped <style>: hero is full-bleed with min-height: 100svh (NEVER vh — iOS CLS), inner .hero__content constrained to max-width 1280px + inline padding var(--space-xl) desktop / var(--space-md) mobile, vertically centered. .hero__bg position:absolute inset:0 z-index:0 overflow:hidden background:var(--color-surface); implement RESEARCH Pattern 1 layered radial-gradient mesh (2-3 soft blobs via ::before/::after in brand tones using rgba derived from brand hex — green rgba(0,132,93,...), faint gold rgba(200,157,41,...), ivory base; set blur ONCE, never inside keyframes), drifting via transform: translate3d() on long mismatched alternate timelines (e.g. 28s/34s ease-in-out). GATE all @keyframes drift behind @media (prefers-reduced-motion: no-preference) so reduce = static gradient. Add static .hero__grain feTurbulence inline-SVG data-URI at opacity ~0.05 (never animated). Hero text color = cream var(--color-surface) anchored over the deeper-green gradient region behind the text band (contrast ≥4.5:1 body / ≥3:1 display — lock blob positions so the worst-case region behind text stays dark). Staged text entrance (RESEARCH Pattern 4): .hero__content > * fade-up opacity 0→1 + translateY(12px→0), ~60ms stagger, --ease-cinematic / --duration-cinematic, transform/opacity only; under reduce set opacity:1 and no translate (defensive). 
    CTA CSS: copy .report-pdf-cta language into .cta--primary (filled var(--color-accent), cream text, font-weight 700 label OK, border-radius 3px, hover → --color-ink + translateY(-1px) + tinted shadow rgba(29,57,55,0.5) + arrow translateX(3px), focus-visible 2px accent offset 3px) and ADD .cta--primary:active { transform: scale(0.98) } (emil press feedback). .cta--outline = same geometry/radius/padding/:active/focus but 1px var(--color-accent) stroke, accent text, transparent fill, hover → accent fill + cream text. .cta-link = quiet inline accent text link (inherits global a), arrow translateX(3px) on hover gated behind @media (hover:hover) and (pointer:fine). Never use transition: all — name exact properties.
    Decide on parallax: ship WITHOUT JS parallax in v1 (spec-compliant "barely perceptible" lower-bound; Phase 6 owns full motion) — the CSS gradient drift is the hero moment. (If parallax is added, it MUST be a tiny is:inline IIFE rAF handler that no-ops under reduced-motion and is absent-safe; do NOT add a half-built version.)
    Responsive: single @media (max-width: 767px) block at the bottom — inline padding var(--space-md); at ≤480px CTA cluster flex-direction:column, each cta width:100% min-height:44px; primary first in DOM/visual order, View the role last. Verify headline does not wrap past 2 lines at 390px (shorten copy if it does — never drop below the --text-display floor). All hex via var() tokens only (decorative gradient/shadow rgba derived from brand hex is the sanctioned exception, matching masthead precedent). Cinzel/Kollektif 400/700 only — never 500/600.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -8 && grep -rq "forms.gle/wUxoKXsZkkEF1N1x5" dist/ && ! grep -rq "forms.gle/placeholder" dist/ && echo "JOIN-URL SMOKE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` succeeds (SSG renders /, no broken-link errors).
    - JOIN-URL build smoke (from 03-VALIDATION.md): `grep -rq "forms.gle/wUxoKXsZkkEF1N1x5" dist/` succeeds AND `grep -rq "forms.gle/placeholder" dist/` finds nothing (combined check prints "JOIN-URL SMOKE PASS").
    - `grep -q "100svh" src/pages/index.astro` succeeds; `grep -c "min-height: 100vh\|min-height:100vh" src/pages/index.astro` returns 0 (no bare vh on hero).
    - `grep -q "Vietnam's markets, read by its sharpest students" src/pages/index.astro` succeeds (locked headline verbatim).
    - `grep -q "A student-led forum for serious equity and macro research on Vietnam" src/pages/index.astro` succeeds (locked subhead verbatim).
    - `grep -q "prefers-reduced-motion" src/pages/index.astro` succeeds; the gradient @keyframes are gated behind `no-preference`.
    - `grep -q "transition: all\|transition:all" src/pages/index.astro` returns nothing (no transition: all).
    - `grep -Eq "font-weight:\s*(500|600)" src/pages/index.astro` returns nothing (no 500/600 weights).
    - `grep -q "scale(0.98)" src/pages/index.astro` succeeds (emil :active press feedback present).
    - `grep -q "View the role" src/pages/index.astro` and `grep -q "Explore work" src/pages/index.astro` both succeed.
  </acceptance_criteria>
  <done>index.astro renders a full-viewport code-driven cinematic hero with the locked copy, three correctly-wired CTAs, reduced-motion-safe gradient, GPU-only motion with emil press feedback, and a mobile block; astro build is green and the Join-URL smoke passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → external Google Form / JD | Outbound navigation to third-party URLs in a new tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `target="_blank"` external links (Join, JD) | mitigate | Every external link carries `rel="noopener noreferrer"` (reverse-tabnabbing) — inherited codebase pattern; enforced on hero CTAs + BaseLayout Join links |
| T-03-02 | Info Disclosure | Member PII | accept | No PII on this page; `past_members.csv` never imported; only public URLs/copy rendered |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: zero new deps; no Package Legitimacy Audit required) |
</threat_model>

<verification>
- `npx astro check` → 0 errors after Task 1.
- `npx astro build` → green after Task 2; SSG renders / with no broken internal links.
- Join-URL smoke: `dist/` contains `forms.gle/wUxoKXsZkkEF1N1x5` and contains NO `forms.gle/placeholder`.
- Manual (owner iPhone UAT, per project memory "verify live in browser"): hero fits one 390px viewport with no scroll, headline ≤2 lines, CTAs visible; reduced-motion (devtools emulate) freezes the gradient; cream-on-deep-emerald text contrast holds at 390px.
</verification>

<success_criteria>
- LND-01: cinematic code-driven hero communicating SEF identity + recruitment value prop, no scroll required, reduced-motion-safe.
- LND-02: prominent Join SEF CTA (hero + inherited header/mobile) → JOIN_FORM_URL in a new tab; placeholder URL fully eliminated from the build.
- All motion is transform/opacity only, GPU-composited, with emil press feedback; no 500/600 weights; var() tokens only.
</success_criteria>

<output>
Create `.planning/phases/03-landing-page/03-01-SUMMARY.md` when done.
</output>
