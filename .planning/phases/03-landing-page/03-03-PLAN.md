---
phase: 03-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: [LND-05]
must_haves:
  truths:
    - "A university logos marquee (Our analysts study at…) renders all 8 delivered logos, grayscale + reduced opacity at rest, in a slow seamless left-to-right auto-scroll driven by the UNIVERSITIES data list"
    - "The marquee uses a duplicate-track translateX 0→-50% linear loop with no visible seam; the duplicate copy is aria-hidden; pause-on-hover works (gated hover:hover)"
    - "Under prefers-reduced-motion the marquee stops and becomes a static wrapped centered row (single copy, duplicate hidden), all 8 logos visible, no horizontal page scrollbar at 390px"
    - "A closing Join band (Be among the best.) reinforces conversion with a single Join SEF CTA + the quiet View the role → link"
    - "Below-the-fold sections fade up on scroll via one shared IntersectionObserver that reveals everything immediately under reduced-motion / no-JS (js-class gated so content is never stuck hidden)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "University marquee (LND-05) + closing Join band + scroll-reveal island appended below featured reports"
      contains: "marquee__track"
  key_links:
    - from: "src/pages/index.astro marquee"
      to: "src/lib/site.ts UNIVERSITIES"
      via: "map over UNIVERSITIES → /logos/universities/${u.file}"
      pattern: "UNIVERSITIES"
    - from: "closing band Join CTA"
      to: "src/lib/site.ts JOIN_FORM_URL"
      via: "href={JOIN_FORM_URL} new tab"
      pattern: "JOIN_FORM_URL"
---

<objective>
Complete the landing page: the university logos marquee social-proof strip (LND-05), the final closing Join band, and a site-restrained scroll-reveal entrance across the below-the-fold sections — then a final mobile + contrast polish pass. After this plan the page is fully shippable end-to-end: a prospective student lands, is drawn through prestige proof and social proof, and is pushed to apply twice; `astro build` is green and the Join smoke passes.

Purpose: LND-05 (university logos strip) is the social-proof close that makes "our analysts study at the best schools" believable; the closing band gives the final conversion push; the scroll-reveal is the quietly-elite entrance discipline that makes the page feel expensive without announcing it.
Output: marquee section + closing band + scroll-reveal `is:inline` island appended to `src/pages/index.astro`, plus the page-wide polish pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-landing-page/03-CONTEXT.md
@.planning/phases/03-landing-page/03-UI-SPEC.md
@.planning/phases/03-landing-page/03-RESEARCH.md
@.planning/phases/03-landing-page/03-PATTERNS.md
@.planning/phases/03-landing-page/03-02-SUMMARY.md
@.agents/skills/emil-design-eng/SKILL.md
@.agents/skills/design-taste-frontend/SKILL.md
@.agents/skills/high-end-visual-design/SKILL.md
@src/styles/tokens.css
@src/styles/global.css

<interfaces>
<!-- Build against these — no scavenger hunt. -->

Data (from Plan 01's site.ts): import { UNIVERSITIES, JOIN_FORM_URL, JD_URL } from '../lib/site';
  UNIVERSITIES = 8 × { name, file } (Berkeley, NYU [new-york-university.webp], UNC, Melbourne, Amsterdam, Marquette,
  Finance & Marketing, VinUniversity). Render <img src={`/logos/universities/${u.file}`} alt={u.name}>.
  All 8 files VERIFIED on disk in public/logos/universities/.

Marquee technique (RESEARCH Pattern 5, lines 243-268) — duplicate-track seamless loop:
  <div class="marquee"><div class="marquee__track">[copy1 real][copy2 aria-hidden]</div></div>
  .marquee { overflow:hidden; background: var(--color-surface-2); }   // mask — page never scrolls sideways
  .marquee__track { display:flex; align-items:center; gap:clamp(var(--space-lg),4vw,var(--space-3xl)); width:max-content; }
  @media (prefers-reduced-motion: no-preference) { .marquee__track { animation: scroll-x 40s linear infinite; }
    @keyframes scroll-x { to { transform: translateX(-50%); } } }   // -50% = exactly one copy
  @media (hover:hover) and (pointer:fine) { .marquee:hover .marquee__track { animation-play-state: paused; } }
  @media (prefers-reduced-motion: reduce) { .marquee__track { flex-wrap:wrap; justify-content:center; width:auto; }
    .marquee__track [aria-hidden="true"] { display:none; } }   // static wrapped row, duplicate hidden
  img: max-height 32-40px, width auto, filter:grayscale(1) opacity:0.55; hover (gated hover:hover) → grayscale(0) opacity:1,
  transition filter/opacity --duration-standard --ease-standard.

Scroll-reveal (RESEARCH Pattern 6, lines 269-292) — one shared IntersectionObserver, is:inline IIFE:
  - .reveal { opacity:0; transform:translateY(16px); transition opacity/transform --duration-slow --ease-cinematic } .reveal.is-visible { opacity:1; transform:none }
  - NO-JS/reduced-motion safety: gate the hidden initial state behind a `.js` class added by an inline head script
    (document.documentElement.classList.add('js')), so non-JS users NEVER see the hidden state; the IO script reveals-all
    immediately under reduce / no-IO. Apply .reveal only to BELOW-the-fold sections (not the hero — Plan 01 owns the hero entrance).

LOCKED copy (UI-SPEC): university framing "Our analysts study at…"; closing heading "Be among the best.";
  primary CTA "Join SEF"; tertiary "View the role →".
External-link rule: Join + View the role get target="_blank" rel="noopener noreferrer"; internal links get neither.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the university logos marquee (LND-05)</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current state after Plan 02 — append below the featured-reports section; extend the single bottom @media block, do not add a second)
    - .planning/phases/03-landing-page/03-RESEARCH.md lines 243-268 (Pattern 5 marquee — use the exact CSS structure) and 345-349 (Pitfall 3: no horizontal page scrollbar)
    - .planning/phases/03-landing-page/03-UI-SPEC.md lines 350-397 (full LND-05 marquee contract incl. reduced-motion static row, sizing, a11y, logo-only rule, mobile)
    - .planning/phases/03-landing-page/03-CONTEXT.md lines 105-112 (logo-normalization note: mixed backgrounds → uniform max-height + consistent padding + grayscale)
    - .agents/skills/emil-design-eng/SKILL.md (constant motion = linear easing; hover:hover gate; transform-only; reduced-motion = stop motion not zero content)
    - .agents/skills/design-taste-frontend/SKILL.md (marquee max-one-per-page — this is the one; logo-only, no category labels under marks; real logos not wordmarks)
  </read_first>
  <action>
    In index.astro frontmatter, ensure UNIVERSITIES is imported from '../lib/site' (add to the existing site import). Append a marquee <section> below the featured-reports section. Structure: a centered framing line "Our analysts study at…" (Kollektif body or small Cinzel --text-heading, --color-ink-muted) above the marquee — NO eyebrow (budget already spent on WSO + featured in Plan 02). Then the marquee per RESEARCH Pattern 5: <div class="marquee"> (overflow:hidden mask, background var(--color-surface-2) tint band) wrapping <div class="marquee__track"> that renders the UNIVERSITIES list TWICE back-to-back — copy 1 is the real list (each <img src={`/logos/universities/${u.file}`} alt={u.name} loading="lazy" decoding="async">), copy 2 is a byte-identical duplicate whose wrapper is aria-hidden="true" (screen readers read each logo once). Both copies must be identical markup so translateX(-50%) loops seamlessly.
    Scoped <style> per the interface block: track is display:flex, gap clamp(var(--space-lg),4vw,var(--space-3xl)), width:max-content; the scroll-x keyframe (translateX 0→-50%, 40s linear infinite) GATED behind @media (prefers-reduced-motion: no-preference); pause-on-hover gated behind @media (hover:hover) and (pointer:fine); under @media (prefers-reduced-motion: reduce) switch the track to flex-wrap:wrap; justify-content:center; width:auto and display:none the aria-hidden duplicate (static wrapped centered row, all 8 visible). Logos: normalize the mixed backgrounds — uniform max-height 32-40px, width auto, consistent vertical centering + padding, filter:grayscale(1) opacity:0.55 at rest, hover lift to grayscale(0) opacity:1 (transition filter/opacity --duration-standard --ease-standard) gated behind hover:hover and pointer:fine. Section padding-block var(--space-4xl) desktop / var(--space-3xl) mobile. Logo-only — NO university name labels printed beneath marks (name lives in alt). animate transform/opacity only; never transition: all. The .marquee overflow:hidden is the mask so the page itself never gets a horizontal scrollbar — verify at 390px.
    Extend the single bottom @media (max-width: 767px) block (do not add a new one): marquee runs the same slow scroll on mobile; the reduced-motion static row wraps to 2-3 tidy rows at 390px with no clipping.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -8 && grep -rq "forms.gle/wUxoKXsZkkEF1N1x5" dist/ && ! grep -rq "forms.gle/placeholder" dist/ && echo "BUILD + JOIN SMOKE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` succeeds.
    - `grep -q "marquee__track" src/pages/index.astro` and `grep -q "UNIVERSITIES" src/pages/index.astro` both succeed (marquee maps the data list).
    - Built page references all 8 logo files: `for f in uc-berkeley new-york-university unc-chapel-hill university-of-melbourne university-of-amsterdam marquette-university university-of-finance-marketing vinuniversity; do grep -q "$f" dist/index.html || echo "MISSING $f"; done` prints nothing.
    - The page renders each logo TWICE (duplicate track): `grep -o "uc-berkeley.png" dist/index.html | wc -l` returns 2.
    - `grep -q "Our analysts study at" src/pages/index.astro` succeeds (locked framing line).
    - `grep -q "grayscale(1)" src/pages/index.astro` succeeds (grayscale at rest); `grep -q "aria-hidden" src/pages/index.astro` succeeds (duplicate hidden from SR).
    - `grep -q "prefers-reduced-motion: reduce\|prefers-reduced-motion:reduce" src/pages/index.astro` succeeds AND the marquee animation is gated behind a no-preference block (`grep -q "prefers-reduced-motion: no-preference\|prefers-reduced-motion:no-preference" src/pages/index.astro`).
    - `grep -q "hover: hover\|hover:hover" src/pages/index.astro` succeeds (hover gated); `grep -q "animation-play-state: paused\|animation-play-state:paused" src/pages/index.astro` succeeds (pause-on-hover).
    - `grep -q "transition: all\|transition:all" src/pages/index.astro` returns nothing; `grep -Eq "font-weight:\s*(500|600)" src/pages/index.astro` returns nothing.
  </acceptance_criteria>
  <done>An 8-logo grayscale duplicate-track marquee driven by UNIVERSITIES scrolls slowly with pause-on-hover, becomes a static wrapped row under reduced-motion, never causes a horizontal page scrollbar, and is the page's only marquee; astro build green; Join smoke passes.</done>
</task>

<task type="auto">
  <name>Task 2: Append the closing Join band + shared scroll-reveal island, then final polish pass</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current state after Task 1 — append the closing band below the marquee; add the reveal island at end of body + the .js head script)
    - src/pages/publications/[id].astro lines 215-252 (.report-pdf-cta language — reuse the .cta--primary built in Plan 01; do not re-style from scratch)
    - .planning/phases/03-landing-page/03-UI-SPEC.md lines 407-412 (closing band contract) and 446-463 (motion contract) and 478-493 (accessibility notes)
    - .planning/phases/03-landing-page/03-RESEARCH.md lines 269-292 (Pattern 6 IntersectionObserver + no-JS .js-class safety) and 355-362 (Pitfall 5: .reveal must not strand content invisible)
    - src/layouts/BaseLayout.astro lines 83-99 (the is:inline IIFE island convention to mirror)
    - .agents/skills/high-end-visual-design/SKILL.md (scroll entry on key sections — heavy gentle fade-up, IntersectionObserver not scroll listeners) and emil (reduced-motion = opacity-only, no translate)
  </read_first>
  <action>
    Append a closing Join band <section> below the marquee: full-width centered band (background var(--color-surface-2) OR a restrained large-area var(--color-brand)/var(--color-accent) fill — large fills are allowed). One Cinzel line "Be among the best." (locked) + the single primary "Join SEF" CTA reusing the .cta--primary class from Plan 01 (filled accent, cream text, <a href={JOIN_FORM_URL} target="_blank" rel="noopener noreferrer">) with the quiet "View the role →" .cta-link to {JD_URL} (new tab) beneath it. NO secondary "Explore work" CTA here — the final push is singular (one intent). If a brand-green/accent fill is used, ensure the CTA + text still clear WCAG (cream text on emerald, or switch the CTA to a cream/ink fill that contrasts the band) — measure, do not guess. padding-block var(--space-4xl) / var(--space-3xl) mobile.
    Add the shared scroll-reveal: an inline head script (is:inline, in the page or via BaseLayout's head slot if available — otherwise a tiny is:inline script at the very top of body) that runs document.documentElement.classList.add('js') so the hidden initial state only applies when JS is present. Add a class="reveal" to the BELOW-the-fold section wrappers (WSO band, value-prop strip, featured reports, marquee, closing band) — NOT the hero (Plan 01 owns the hero entrance). Scoped CSS: gate the hidden state behind .js — `.js .reveal { opacity:0; transform:translateY(16px); transition: opacity var(--duration-slow) var(--ease-cinematic), transform var(--duration-slow) var(--ease-cinematic); }` and `.reveal.is-visible { opacity:1; transform:none; }`. At the end of body add the is:inline IIFE IntersectionObserver from RESEARCH Pattern 6: if no IO support OR matchMedia('(prefers-reduced-motion: reduce)').matches → add .is-visible to all .reveal immediately and return; else observe with threshold 0.15, rootMargin '0px 0px -10% 0px', add .is-visible + unobserve on intersect. Transform/opacity only.
    Final polish pass (no new sections — tighten what exists across all of index.astro): (1) confirm section order is hero → WSO → value-prop → featured → marquee → closing (UI-SPEC composition) and no two adjacent sections share a layout family; (2) confirm exactly 2 eyebrows on the page (WSO + featured), one primary intent ("Join SEF") and one secondary ("Explore work"), the JD link is a quiet text link not a button; (3) confirm macro-whitespace rhythm via --space-4xl/5xl between sections; (4) confirm all hex are var() tokens (decorative rgba derived from brand hex is the sanctioned exception); (5) confirm Cinzel/Kollektif 400/700 only; (6) ensure CTAs ≥44px touch target and external links carry rel="noopener noreferrer"; (7) re-confirm the single bottom @media (max-width:767px) block covers every section for 390px with no horizontal scrollbar.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -8 && grep -rq "forms.gle/wUxoKXsZkkEF1N1x5" dist/ && ! grep -rq "forms.gle/placeholder" dist/ && echo "BUILD + JOIN SMOKE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` succeeds; Join-URL smoke passes (dist/ has forms.gle/wUxoKXsZkkEF1N1x5, no forms.gle/placeholder) — prints "BUILD + JOIN SMOKE PASS".
    - `grep -q "Be among the best" src/pages/index.astro` succeeds (locked closing heading).
    - The page has TWO Join SEF CTAs wired to the form (hero + closing): `grep -o "forms.gle/wUxoKXsZkkEF1N1x5" dist/index.html | wc -l` returns >= 2.
    - `grep -q "IntersectionObserver" src/pages/index.astro` succeeds; `grep -q "classList.add('js')\|classList.add(\"js\")" src/pages/index.astro` succeeds (js-class no-JS guard); `grep -q ".js .reveal\|\.js \.reveal" src/pages/index.astro` succeeds (hidden state gated behind .js).
    - `grep -q "prefers-reduced-motion" src/pages/index.astro` succeeds and the reveal IIFE reveals-all under reduce.
    - `grep -c "eyebrow" src/pages/index.astro` shows ≤2 eyebrow instances; `grep -c "Explore work" src/pages/index.astro` returns 1 (single secondary intent, only in hero).
    - `grep -q "transition: all\|transition:all" src/pages/index.astro` returns nothing; `grep -Eq "font-weight:\s*(500|600)" src/pages/index.astro` returns nothing.
    - `grep -c "@media (max-width: 767px)\|@media (max-width:767px)" src/pages/index.astro` returns 1 (single mobile block).
  </acceptance_criteria>
  <done>Closing Join band (locked heading + single Join CTA + quiet JD link) and a no-JS-safe shared IntersectionObserver scroll-reveal are in place; the full 6-section page passes the final polish checklist; astro build green and the page has ≥2 working Join CTAs with no placeholder URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → external Google Form / JD | Outbound new-tab navigation (closing band Join + JD link) |
| browser → static logo assets | Decorative public images, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | closing-band external links | mitigate | rel="noopener noreferrer" + target="_blank" on Join + View the role (reverse-tabnabbing) |
| T-03-06 | Denial of Service | marquee + reveal animations on low-power devices | mitigate | transform/opacity-only GPU motion; reduced-motion stops marquee + reveals; CSS off-main-thread |
| T-03-07 | Info Disclosure | university logos | accept | Public institution logos + names only; no member PII; past_members.csv never imported |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new deps) |
</threat_model>

<verification>
- `npx astro build` green; Join-URL smoke passes (regression guard for the whole page).
- Manual (owner iPhone UAT, the phase gate per project memory): all 8 logos read as one cohesive grayscale family and loop seamlessly with pause-on-hover; reduced-motion (devtools emulate) stops the marquee → static wrapped row and freezes the hero gradient; no horizontal scrollbar at 390px; below-the-fold sections fade up on scroll and are NEVER stuck invisible with JS disabled; the closing band gives a clean final Join push; the whole page answers "does this make you want to apply?".
- Contrast spot-check (BRD-03): hero text + any colored closing-band fill measured ≥4.5:1 body / ≥3:1 large at desktop and 390px.
</verification>

<success_criteria>
- LND-05: 8-logo grayscale duplicate-track marquee from the UNIVERSITIES data list, slow seamless scroll, pause-on-hover, reduced-motion → static wrapped row, no horizontal page scrollbar, logo-only.
- Closing Join band reinforces LND-02 with a single Join SEF CTA + quiet JD link.
- Site-restrained scroll-reveal entrance (one shared IntersectionObserver, no-JS-safe) on below-the-fold sections; final polish checklist (section order, eyebrow budget, whitespace rhythm, tokens, weights, touch targets, single mobile block) all pass.
- The complete 6-section landing page is shippable: cinematic, quietly-elite, institution-grade, with two working Join paths.
</success_criteria>

<output>
Create `.planning/phases/03-landing-page/03-03-SUMMARY.md` when done.
</output>
